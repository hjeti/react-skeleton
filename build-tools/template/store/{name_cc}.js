import { handleActions } from 'redux-actions';

// Actions
const namespace = '{{name_cc}}';

// Reducer
const initialState = {};

const {{name_cc}}Reducer = handleActions({}, initialState);

export default {{name_cc}}Reducer;

// Action creators

// Thunks

// Selectors
